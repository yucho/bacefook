import React from 'react';

const prepend = 'right-sidebar-friends-suggestion';
const FriendsSuggestion = () => {
  return <section className={`${prepend}-container`}>
  </section>
};

export default FriendsSuggestion;
