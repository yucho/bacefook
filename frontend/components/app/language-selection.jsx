import React from 'react';

const prepend = 'right-sidebar-language-selection';
const LanguageSelection = () => {
  return <section className={`${prepend}-container`}>
    <ul>
      <li>English (US)</li>
    </ul>
  </section>
};

export default LanguageSelection;
