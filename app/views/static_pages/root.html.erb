<% script = "" %>
<%
if logged_in?
script += <<-JS
  window.bacefook.currentUser = #{current_user.id}
  window.bacefook.preloadedState = #{{path: "/"}.to_json}
JS
end
if true
script += <<-JS
  window.bacefook.requestedPath = '#{request.env['PATH_INFO']}'
JS
end
%>

<% unless script.empty? %>
<script>
  window.bacefook = {
    images: <%= image_paths %>
  }
<%= script.html_safe %>
</script>
<% end %>

<div id="root">React is broken</div>
